@model IEnumerable<ZaKhan.Model.ProdBrandView>

@{
    ViewBag.Title = "Index";
}


<link href="~/Content/Site.css" rel="stylesheet" />
<div class="panel panel-default">
    <div class="panel-heading" align="center">
        <h2>All Brands</h2>
    </div>
    <div class="panel-body">
        @Html.ActionLink("New Brand", "Create", null, new { @class = "btn btn-primary btn-sm" })
        <table class="table table-striped table-condensed table-bordered">
            <tr>
                <th class="text-left">Brand Name</th>

            </tr>
            @foreach (var item in Model)
            {
                <tr>

                    <td>@item.BrandName</td>

                    <td>
                        <div align="right">
                            @Html.ActionLink("Edit", "Update", new {id = item.BrandId}, new {@class = "btn btn-warning"})
                            @Html.ActionLink("Details", "Details", new {id = item.BrandId}, new {@class = "btn btn-primary"})
                            @Html.ActionLink("Delete", "Delete", new {id = item.BrandId}, new {@class = "btn btn-danger"})
                        </div>
                    </td>
                </tr>
            }
        </table>
        @Html.ActionLink("Cancel and return to List", "Admin", "Login", null, new
                    {
                        @class = "btn btn-success"
                    })
    </div>
</div>
